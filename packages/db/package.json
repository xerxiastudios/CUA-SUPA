{
  "name": "@my/db",
  "packageManager": "yarn@3.2.3",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "drizzle:studio": "yarn with-env drizzle-kit studio",
    "drizzle:generate": "yarn with-env drizzle-kit generate:pg",
    "drizzle:migrate": "yarn with-env drizzle-kit generate:pg",
    "drizzle:migrate:dev": "yarn with-env drizzle-kit generate:pg",
    "clean": "rm -rf .turbo node_modules",
    "with-env": "dotenv -e ../../.env --",
    "db-push": "yarn db-generate",
    "db-generate": "yarn with-env drizzle-kit generate:pg"
  },
  "dependencies": {
    "drizzle-orm": "^0.27.2",
    "postgres": "^3.3.5"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "drizzle-kit": "^0.19.12",
    "typescript": "^5.1.6"
  }
}
